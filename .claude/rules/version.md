# Versioning Rules

- When bumping the version number, update the `version` field in `package.json` and `package-lock.json`
- When the version number is updated, update the `CHANGELOG.md` file with the new version number, release date and the changes
